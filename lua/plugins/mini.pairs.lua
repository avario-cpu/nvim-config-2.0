return {
  "echasnovski/mini.pairs",
  opts = function(_, opts)
    -- Your other mini.pairs settings here
    opts.modes = {
      insert = true,
      command = false,
      terminal = false,
    }
  end,
  enalbed = true,
}
