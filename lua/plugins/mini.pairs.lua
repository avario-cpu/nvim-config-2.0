return {
  "echasnovski/mini.pairs",
  enabled = true,
  opts = function(_, opts)
    -- Your other mini.pairs settings here
    opts.modes = {
      insert = true,
      command = false,
      terminal = false,
    }
  end,
  enalbed = true,
}
